fn fibonacci(n: i32) -> i32 {
    if (n <= 1) {
        return n;
    }
    return fibonacci(n - 1) + fibonacci(n - 2);
}

fn main() {
    for (i = 0; i < 10; i = i + 1) {
        print("fib(", i, ") = ", fibonacci(i));
    }
}
